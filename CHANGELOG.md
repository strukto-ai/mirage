# Changelog

## Unreleased

- Add injectable clocks for RAM file-cache TTLs, FUSE prefetch expiry, and op-facade timing in Python and TypeScript, with manual time advancement and clock propagation through workspace copies and snapshot restoration.
- Preserve an inline refusal for the immediate retry so the approval channel does not ask the same question twice.
