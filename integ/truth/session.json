{
  "runs": {
    "python": {
      "command": ["./python/.venv/bin/python", "examples/python/permissions/session.py"]
    },
    "typescript": {
      "command": ["pnpm", "-C", "examples/typescript", "exec", "tsx", "permissions/session.ts"]
    }
  },
  "exit": 0,
  "lines": [
    "reviewer  shell     cat /repo/README.md                [0] hello repo",
    "                                                       the shell door, as the reviewer",
    "reviewer  shell     cat /repo/secrets/key.pem          [1] cat: /repo/secrets/key.pem: No such file or directory",
    "                                                       the reviewer's profile hides the directory",
    "editor    shell     cat /repo/secrets/key.pem          [1] cat: /repo/secrets/key.pem: keys are never read by hand",
    "                                                       the editor's rule denies the file by name",
    "reviewer  vfs.read  /repo/secrets/key.pem              ENOENT",
    "                                                       the op door, the same hide, the same answer",
    "editor    vfs.read  /repo/secrets/key.pem              EACCES",
    "                                                       the op door, the same rule, the same answer",
    "host      vfs.read  /repo/secrets/key.pem              PRIVATE",
    "                                                       no default profile: the workspace's own door sees it",
    "host      vfs.read  /repo/secrets/key.pem as reviewer  ENOENT",
    "                                                       the same door, named per call: the reviewer's hide",
    "host      vfs.read  /repo/secrets/key.pem as editor    EACCES",
    "                                                       and the editor's own rule, from the same call site",
    "reviewer  vfs.write /repo/new.txt                      EROFS",
    "                                                       the reviewer's handle caps /repo at read",
    "editor    vfs.write /repo/new.txt                      ok",
    "                                                       the editor may write",
    "reviewer  vfs.read  /repo/new.txt                      editor wrote",
    "                                                       one world: the reviewer reads what the editor wrote",
    "reviewer  shell     echo x > /repo/new.txt             [1] /repo/new.txt: Read-only file system",
    "                                                       the shell door reads the same cap",
    "editor    shell     echo x > /repo/new.txt             [0]",
    "                                                       and the same grant",
    "reviewer  session   ws.session('reviewer')             reviewer",
    "                                                       an existing session is adopted as is",
    "reviewer  session   ws.session('reviewer', profile=...) refused: session 'reviewer' exists; its profile was set when it was created",
    "                                                       a profile is set once, at creation"
  ]
}
